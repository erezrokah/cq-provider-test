-- Autogenerated by migration tool on 2022-01-19 12:20:18

-- Resource: error_resource
CREATE TABLE IF NOT EXISTS "error_resource" (
	"cq_id" uuid NOT NULL,
	"cq_meta" jsonb,
	"cq_fetch_date" timestamp without time zone NOT NULL,
	CONSTRAINT error_resource_pk PRIMARY KEY(cq_fetch_date,cq_id),
	UNIQUE(cq_fetch_date,cq_id)
);
SELECT setup_tsdb_parent('error_resource');

-- Resource: migrate_resource
CREATE TABLE IF NOT EXISTS "migrate_resource" (
	"cq_id" uuid NOT NULL,
	"cq_meta" jsonb,
	"cq_fetch_date" timestamp without time zone NOT NULL,
	"upgrade_column" integer,
	"upgrade_column_2" integer,
	CONSTRAINT migrate_resource_pk PRIMARY KEY(cq_fetch_date,cq_id),
	UNIQUE(cq_fetch_date,cq_id)
);
SELECT setup_tsdb_parent('migrate_resource');

-- Resource: panic_resource
CREATE TABLE IF NOT EXISTS "panic_resource" (
	"cq_id" uuid NOT NULL,
	"cq_meta" jsonb,
	"cq_fetch_date" timestamp without time zone NOT NULL,
	CONSTRAINT panic_resource_pk PRIMARY KEY(cq_fetch_date,cq_id),
	UNIQUE(cq_fetch_date,cq_id)
);
SELECT setup_tsdb_parent('panic_resource');

-- Resource: slow_resource
CREATE TABLE IF NOT EXISTS "slow_resource" (
	"cq_id" uuid NOT NULL,
	"cq_meta" jsonb,
	"cq_fetch_date" timestamp without time zone NOT NULL,
	"some_bool" boolean,
	CONSTRAINT slow_resource_pk PRIMARY KEY(cq_fetch_date,cq_id),
	UNIQUE(cq_fetch_date,cq_id)
);
SELECT setup_tsdb_parent('slow_resource');

-- Resource: very_slow_resource
CREATE TABLE IF NOT EXISTS "very_slow_resource" (
	"cq_id" uuid NOT NULL,
	"cq_meta" jsonb,
	"cq_fetch_date" timestamp without time zone NOT NULL,
	CONSTRAINT very_slow_resource_pk PRIMARY KEY(cq_fetch_date,cq_id),
	UNIQUE(cq_fetch_date,cq_id)
);
SELECT setup_tsdb_parent('very_slow_resource');
